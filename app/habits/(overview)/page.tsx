import React from 'react'

import HabitTable from '@/app/ui/habits/habit-check';
import { getUser, getHabits, getHabitResults, printUserHabitsAndResults } from '../../lib/data';
import { User, Habit, HabitResult } from '../../lib/definitions';




export default async function Table() {
//   const email:string = 'john.doe@example.com'
//   const user:User = await getUser(email);
//   const habits:Habit[] = await getHabits(user.id);
//   const habitResults:HabitResult[][] = await getHabitResults(habits.map((habit) => habit.id));
  
  return (
    <div>
      {/* <HabitTable habits = {habits} habitResults = {habitResults}/> */}
    </div>
    
  );
}

